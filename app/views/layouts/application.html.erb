<!DOCTYPE html>
<html>
  <head>
    <title>Catalyst Support</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
    <link rel="shortcut icon" href="favicon-16x16.png" type="image/png">
    <link rel="shortcut icon" href="favicon-32x32.png" type="image/png">
    <link rel="shortcut icon" href="favicon-96x96.png" type="image/png">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="flex flex-col h-screen">
    <header>
      <nav class="bg-brand-navy border-b-8 border-brand-green">
        <div class="outer-container py-5 flex justify-between">
          <%= link_to root_path do %>
            <%= image_tag('logo-white.svg', height: 24, width: 87) %>
          <% end %>
          <div>
            <%= link_to 'Home', root_path, class: "text-white font-semibold hover:underline mr-5#{active_nav?(controller: 'statics', action: 'index')}" %>
            <%= link_to 'Organisations', organisations_path, class: "text-white font-semibold hover:underline mr-5#{active_nav?(controller: 'organisations', action: 'index')}" %>
            <%= link_to 'Support requests', tickets_path, class: "text-white font-semibold hover:underline mr-5#{active_nav?(controller: 'tickets', action: 'index')}" %>
          </div>
        </div>

        <p><%= notice %></p>
        <p><%= alert %></p>
      </nav>

      <div class="outer-container">
        <div class="py-2 border-b">
          <div class="inline-flex text-white text-xs font-semibold tracking-widest uppercase bg-phase-alpha px-3 py-1 mr-1">Alpha</div>
          This is a new service – your <a href="mailto:hello@thecatalyst.org.uk?subject=Catalyst%20Support%20Feedback" class="underline">feedback</a> will help us to improve it.
        </div>
      </div>
    </header>

    <main class="flex-1">
      <div class="outer-container">
        <%= yield %>
      </div>
    </main>

    <footer  class="bg-brand-navy-5">
      <div class="outer-container py-8">
        <%= image_tag('logo-navy.svg', height: 40, width: 123) %>
      </div>
    </footer>
  </body>
</html>
